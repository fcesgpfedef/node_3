const str = 'Hyderabad';

const resSplit = str.split('');
const resReverse = resSplit.reverse();
const resJoin = resReverse.join('');
console.log(str);
console.log(resJoin);

// Write same program of revering a string without using JS inbuilt functions.